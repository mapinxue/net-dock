{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main Net Dock window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
